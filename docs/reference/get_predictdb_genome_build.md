# Get genome build of PredictDB weight

Get genome build of PredictDB weight

## Usage

``` r
get_predictdb_genome_build(weight_file)
```

## Arguments

  - weight\_file:
    
    a string, pointing path to weights in PredictDB format.
