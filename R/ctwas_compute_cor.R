# Compute gene-gene and gene-SNP correlations
compute_cor <- function(region_info,
                        gene_info,
                        weight,
                        select = NULL,
                        thin = 1,
                        minvar = 2,
                        outputdir = getwd(),
                        outname = NULL,
                        merge = FALSE,
                        ncore = 1){

}

